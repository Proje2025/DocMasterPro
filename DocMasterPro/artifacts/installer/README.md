# Installer artifacts

Installers are produced by the GitHub Actions release workflow and published on the GitHub Releases page.

Use the latest `DocMasterPro-win-Setup.exe` release asset instead of committing generated setup binaries to the repository.
